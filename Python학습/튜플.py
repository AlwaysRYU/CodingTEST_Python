# 튜플
'''
의미:
    () 를 사용한다.
    괄호를 사용하지 않아도 된다.
    튜플은 그 값을 바꿀 수 없다.
특징:
    그 값을 바꿀 수 없다. (수정, 삭제 불가)
    슬라이싱, 추가(곱하기), 인덱싱은 가능하다.
    길이함수도 가능하다.
    순서가 있다.
'''

#사용하기
tuple1 = (1,2,'a','b')
print(tuple1[0])
print(len(tuple1))